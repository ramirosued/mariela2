{
  "name": "recursero-digital",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend/recursero-digital && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
